# Nexora Local Models

Nexora is designed for local-first model execution.

For Phase 1, model files are not stored in this repository. Ollama manages local
model downloads separately, and future llama.cpp support can use this directory
for local GGUF model references or notes.

Configured Ollama tags are listed in `configs/model_config.yaml`.
